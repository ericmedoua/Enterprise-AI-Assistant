apiVersion: v1
kind: Service

metadata:
  name: enterprise-ai-service
  namespace: enterprise-ai

spec:
  selector:
    app: enterprise-ai-api

  ports:
    - port: 8000
      targetPort: 8000

  type: NodePort